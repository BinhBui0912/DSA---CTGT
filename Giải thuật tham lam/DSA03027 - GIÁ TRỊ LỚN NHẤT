#include <bits/stdc++.h>

using namespace std;

using ll = long long;

void inp(){
	int n; cin >> n;
	int a[n];
	for(int i = 0; i < n; i++) cin >> a[i];
	ll ans = 0;
	for(int i = 0; i < n; i++){
		ans += a[i] + abs(a[i]);
	}
	cout << ans << endl;
} 

int main(){
	inp();
	return 0;
}
