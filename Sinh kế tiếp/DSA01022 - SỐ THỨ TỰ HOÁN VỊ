#include<bits/stdc++.h>

using namespace std;

using ll = long long;

int main(){
	int t; cin >> t;
	while(t--){
		int n; cin >> n;
		int a[1000];
		for(int i = 0; i < n; i++) cin >> a[i];
		int cnt = 1;
		while(prev_permutation(a, a+n)){
			cnt++;
		}
		cout << cnt << endl;
	} 
} 
