- upper_bound(iterator 1, iterator 2, x) : trả về con trỏ(iterator) đến phần tử đầu tiên trong mảng
> x,  nếu không tìm được sẽ trả về iterator 2;

auto it = upper_bound(iterator 1, iterator 2, x):
- in ra phần tử tìm được: cout << *it;
- in ra vị trí phần tử: cout << it - a.begin();
                        cout << distance(a.begin(), it);  // hàm trả về khoảng cách 2 iterator;
